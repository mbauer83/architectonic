---
source-entity: SRV@1780656241.ooK3YN.assurance-service
version: 0.1.0
status: draft
last-updated: '2026-06-20'
---

<!-- §connections -->

### archimate-realization → REQ@1780655839.kjBJrh.assurance-to-architecture-linkage

The assurance service enforces analysis anchors, one-way bindings, computed reverse navigation, and modelling-gap workflows.

### archimate-realization → REQ@1780655839.JpAJkO.assurance-as-a-separate-module-family

The Assurance Service realizes this by being a separate module family with its own store, ontology and capability gate, so a deployment without the confidential store simply does not load it.

### archimate-realization → REQ@1780655839.az20QC.guidance-first-assurance-method-support

The Assurance Service realizes this by serving per-topic method guidance alongside every analysis surface, so the method leads the author rather than the author having to know it.

### archimate-realization → REQ@1785058330.-LmyST.component-failure-analysis-attaches-to-the-existing-hazard-chain

The Assurance Service realizes this by modelling a failure mode's consequence as an edge to a hazard the system analysis already states, so FMEA adds a row per component instead of a second vocabulary of effects and severities.

### archimate-serving → FNC@1780656241.elLBp7.guide-assurance-method-standards

Assurance method guidance comes from the assurance service; whoever applies it is performing the method, not the platform.

### archimate-serving → PRC@1784532411.JMfieH.ingest-security-signals

The agent performs the ingestion; the assurance service is what it ingests through. The requirement is met by the connector behind that service, which is why the connector realizes it and this serves.
