---
source-entity: GRP@1786958823.Rc3mGTi.write-pipeline
version: 0.1.0
status: draft
last-updated: '2026-08-17T10:10:53Z'
---

<!-- §connections -->

### archimate-aggregation → APP@1712870400.kRZYOA.architecture-mcp-endpoint-adapter

The MCP surface a write arrives on.

### archimate-aggregation → APP@1776637531.a9k_mJ.write-request-queue

Admission and serialization: every write is accepted immediately and executed through one drain worker.

### archimate-aggregation → APP@1777399924.5n_KQJ.bulk-write-handler

Transactional execution of bulk creates and edits.

### archimate-aggregation → APP@1777399925.WK_78v.bulk-delete-handler

Transactional execution of bulk deletes, including the preflight that refuses one another artifact references.

### archimate-aggregation → APP@1777399926.PDJI8F.operation-registry

Idempotency: a repeated call returns the first result rather than doing the work twice.

### archimate-aggregation → APP@1777399925.HOOYAQ.staged-write-transaction-manager

The all-or-nothing boundary for any change spanning more than one file.

### archimate-aggregation → APP@1712870400.ca3vm7.model-verifier

The gate a staged change must pass before it is published. Also part of the read concern, which is why membership is aggregation.

### archimate-aggregation → APP@1781976554.AeU7V3.identifier-allocation-service

Allocates the identifiers a new artifact is written under.

### archimate-aggregation → FNC@1777390460.3ZsfnT.broadcast-model-update

Announces a completed write so readers refresh.

### archimate-aggregation → APP@1777293134.tY3JIK.git-sync-service

Publishes a committed change to the remote and pulls others' changes in — the last step of the write path, and the one that makes a write visible outside this workspace.
