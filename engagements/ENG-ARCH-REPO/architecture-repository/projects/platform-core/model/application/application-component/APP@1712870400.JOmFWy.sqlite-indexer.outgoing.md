---
source-entity: APP@1712870400.JOmFWy.sqlite-indexer
version: 0.1.0
status: draft
last-updated: '2026-04-27T00:00:00Z'
---

<!-- §connections -->

### archimate-access → DOB@1712870400.3rilik.sqlite-index

```yaml
access-type: write
```

The SQLite indexer writes entity and connection metadata to the SQLite index.

### archimate-serving → APP@1712870400.yNhgdh.module-catalog

SQLite Indexer populates and incrementally updates the in-memory store that the Model Registry exposes for fast entity and connection lookups.

### archimate-realization → REQ@1712870400.Cc2Dd2.semantic-full-text-search

The SQLite indexer fulfils semantic full-text search by building and maintaining the FTS-backed read index every search runs against.

### archimate-realization → REQ@1785945042.SNEDRG.cross-tier-coherence-verification

The indexer resolves references across both tiers, which is what the check reads.

### archimate-serving → APP@1777293133.OYEmP1.architecture-backend

The backend holds its canonical index instances — at most one per repository root.

### archimate-serving → APP@1786793392.Ir9SkCr.workspace-administration-toolkit

arch-exchange builds an index of its own to resolve what it exports.
