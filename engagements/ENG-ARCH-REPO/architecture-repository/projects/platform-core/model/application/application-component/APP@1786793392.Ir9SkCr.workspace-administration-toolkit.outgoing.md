---
source-entity: APP@1786793392.Ir9SkCr.workspace-administration-toolkit
version: 0.1.0
status: active
last-updated: '2026-08-15T11:31:44Z'
---

<!-- §connections -->

### archimate-assignment → AIF@1786793394.ZVeJv9r.workspace-administration-interface

The toolkit exposes the Repository Administration Interface. No serving relation from the backend reaches it: what is reachable here acts on the repository directly.

### archimate-composition → APP@1783872532.OZbaBh.repository-upgrade-framework

arch-repair upgrade is carried by the toolkit.

### archimate-composition → APP@1783870966.p8Tjw_.model-exchange-adapter

arch-exchange is carried by the toolkit.

### archimate-assignment → FNC@1783870976.MlO3ST.load-guidance-content

```yaml
specialization: behavior-assignment
```

arch-import-guidance performs guidance loading, reading the import source into the per-deployment cache. Carried by the toolkit rather than a component of its own: the entry point has no other modelled behaviour.

### archimate-composition → APP@1776633694.w51oVF.workspace-initializer

arch-init is a toolkit command, and arch-switch-engagement reuses the initializer's internals. Everything else — the backend included — imports only load_init_state, reading the workspace state file rather than performing initialization; the backend's relationship to it is its access to Workspace Configuration.
