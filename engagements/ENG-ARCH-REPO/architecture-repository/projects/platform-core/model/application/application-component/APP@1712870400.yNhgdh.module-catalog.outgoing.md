---
source-entity: APP@1712870400.yNhgdh.module-catalog
version: 0.1.0
status: draft
last-updated: '2026-07-12'
---

<!-- §connections -->

### archimate-composition → APP@1781705596.n8Pikk.datatype-diagram-type-module

Module Catalog is composed of the Datatype Diagram Type Module, loaded at startup.

### archimate-composition → APP@1782027168.bARYLw.gsn-diagram-type-module

The module catalog discovers and exposes the GSN diagram-type module.

### archimate-realization → REQ@1712870400.KeGCZE.archimate-4-0-model-ontology

The Module Catalog realizes this by holding the loaded ArchiMate 4.0 ontology — entity types, connection types, specializations and permitted relationships — as the immutable catalog every read and write resolves against.

### archimate-realization → REQ@1777369404.aDohcf.extensibility-configurability

The Module Catalog realizes this by loading ontologies and diagram types declaratively at startup, so a new entity type, connection type or notation is configuration rather than code.

### archimate-serving → APP@1786225304.SpexYvB.scratchpad-service

The Module Catalog supplies the classification hierarchy refinement walks: which domains a meta-ontology declares, which entity types sit in each, and which specializations narrow a type. The scratchpad names no ArchiMate level of its own, so a meta-ontology of another shape is followed without change.

### archimate-serving → APP@1786225304.q6wT1kq.scratchpad-link-verifier

The same catalog answers whether a link is permitted between two entity types, so narrowing and verification read one source rather than two copies of the rules.

### archimate-realization → REQ@1786230066.vVjYCpP.declare-a-meta-ontology-s-classification-levels-in-the-ontology-module

The Module Catalog is where the declared levels are read from and served to every consumer.

### archimate-serving → APP@1777293133.OYEmP1.architecture-backend

Built once at startup and injected through RuntimeCatalogs.

### archimate-serving → APP@1786793392.Ir9SkCr.workspace-administration-toolkit

arch-repair and arch-import-guidance build the catalog through app_bootstrap without a backend.
