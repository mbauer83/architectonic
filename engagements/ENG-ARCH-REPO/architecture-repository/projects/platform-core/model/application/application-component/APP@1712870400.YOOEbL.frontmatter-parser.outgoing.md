---
source-entity: APP@1712870400.YOOEbL.frontmatter-parser
version: 0.1.0
status: draft
last-updated: '2026-04-27T00:00:00Z'
---

<!-- §connections -->

### archimate-access → DOB@1777293139.UjyXG3.architecture-ontology-configuration

Frontmatter Parser reads the ontology configuration to validate that artifact-type values in parsed files are members of the defined type set.

### archimate-realization → REQ@1712870400.Aa1Bb1.obligatory-structured-frontmatter-for-artifacts

The frontmatter parser fulfils the obligatory-structured-frontmatter requirement: it parses and enforces the YAML frontmatter contract on every artifact read.

### archimate-realization → REQ@1712870400.V5EdQk.markdown-format

The Frontmatter Parser realizes this by reading and writing the markdown-with-YAML-frontmatter form every artifact is stored in.

### archimate-realization → REQ@1712870400.vlMSrd.file-based-artifacts

The Frontmatter Parser realizes this by making one file the unit of an artifact, so the repository's state is its files and nothing is held only in an index.

### archimate-serving → APP@1777293133.OYEmP1.architecture-backend

The backend reads frontmatter through it.

### archimate-serving → APP@1786793392.Ir9SkCr.workspace-administration-toolkit

arch-repair upgrade reads frontmatter in its own process, with no backend running.
