---
source-entity: NOD@1782080479.7xNYBF.team-serving-deployment
version: 0.1.0
status: draft
last-updated: '2026-08-16T21:20:38Z'
---

<!-- §connections -->

### archimate-serving → APP@1780783671.hkrdtm.architecture-management-platform

Team-Serving Deployment hosts the Architecture Management Platform as a long-lived network-bound process, serving concurrent GUI sessions and MCP agent clients over the network.

### archimate-composition → NOD@1786915205.Yx71j_C.app-container

The team-serving host runs the container; the container has no existence apart from the host that runs it, which is why this is composition rather than aggregation.

### archimate-composition → NOD@1786915451.THkLB12.repository-data-volume

The host provides the repository volume; it has no existence apart from the deployment that declares it.

### archimate-composition → NOD@1786915451.pVqArvF.assurance-store-volume

The host provides the assurance volume.

### archimate-composition → NOD@1786915451.l1ntGpZ.backend-state-volume

The host provides the backend state volume.

### archimate-composition → NOD@1786915451.IwBEhEm.credential-vault-volume

The host provides the home volume that carries the credential vault.

### archimate-composition → NOD@1786915451.yVcsZO4.tls-reverse-proxy

The proxy runs on the same host, started by an optional compose profile.

### archimate-aggregation → SSW@1780656477.kIOyT2.pocketbase

Optional deployment profile: PocketBase runs on the same host as an alternative backend for the assurance store.
