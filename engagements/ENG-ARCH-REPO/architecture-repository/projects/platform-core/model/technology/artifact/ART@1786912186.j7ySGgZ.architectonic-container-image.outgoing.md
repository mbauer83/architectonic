---
source-entity: ART@1786912186.j7ySGgZ.architectonic-container-image
version: 0.1.0
status: draft
last-updated: '2026-08-16T20:30:03Z'
---

<!-- §connections -->

### archimate-realization → APP@1777293133.OYEmP1.architecture-backend

The image carries the backend's Python environment and source layout, so running the image is what makes the backend exist at runtime.

### archimate-realization → APP@1776149382.lmO0mp.gui-authoring-tool

The SPA is built into the image in its first stage and served by the backend from it, so the authoring tool ships inside the same image rather than being deployed separately.
